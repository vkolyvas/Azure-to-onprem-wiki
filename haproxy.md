# HAProxy – Installation & Configuration
**Installation**
```apt install haproxy```

**Configuration**

Edit ```/etc/haproxy/haproxy.cfg``` to define:

- Frontends

- Backends

- Health checks

Restart service.
